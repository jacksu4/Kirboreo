# 📊 FOMO Meter 新闻情绪判断逻辑

## 🎯 核心原理

新闻情绪通过**加权评分系统**判断，而不是简单的关键词匹配。

---

## 🔢 评分机制

每条新闻标题根据包含的关键词累计得分：

| 关键词类型 | 得分 | 说明 |
|-----------|------|------|
| 强看涨词 | **+2** | surge, rocket, breakthrough 等 |
| 一般看涨词 | **+1** | rise, gain, upgrade 等 |
| 强看跌词 | **-2** | crash, plunge, collapse 等 |
| 一般看跌词 | **-1** | fall, drop, concern 等 |

---

## 📋 完整关键词列表

### 🔴 看涨 (Bullish) 关键词

**强看涨 (+2 分)：**
```
soar, surge, rocket, skyrocket, breakthrough, 
record high, all-time high, rally, boom
暴涨, 飙升, 突破, 创新高
```

**一般看涨 (+1 分)：**
```
rise, gain, grow, up, bullish, positive, boost, 
upgrade, beat, outperform, strength
上涨, 看涨, 增长, 利好
```

### 🟢 看跌 (Bearish) 关键词

**强看跌 (-2 分)：**
```
crash, plunge, collapse, tank, plummet, 
crisis, disaster
崩盘, 暴跌, 危机
```

**一般看跌 (-1 分)：**
```
fall, drop, decline, down, bearish, negative, 
concern, worry, miss, underperform, weakness, 
cut, downgrade
下跌, 看跌, 担忧, 利空
```

---

## 🎯 判断规则

```
总分 ≥ 2  → 🔴 看涨 (bullish)
总分 ≤ -2 → 🟢 看跌 (bearish)
-1 ≤ 总分 ≤ 1 → 🟡 中性 (neutral)
```

### 例子

#### 例 1：看涨新闻
**标题：** "Tesla Stock **Surges** After Earnings **Beat** Expectations"

**计算：**
- `surge` (强看涨) → **+2**
- `beat` (一般看涨) → **+1**
- **总分 = +3 ≥ 2** → 🔴 **看涨**

---

#### 例 2：看跌新闻
**标题：** "Nvidia Shares **Plunge** on **Concerns** About AI Demand"

**计算：**
- `plunge` (强看跌) → **-2**
- `concerns` (一般看跌) → **-1**
- **总分 = -3 ≤ -2** → 🟢 **看跌**

---

#### 例 3：中性新闻
**标题：** "Apple Announces New Partnership with OpenAI"

**计算：**
- 无看涨或看跌关键词
- **总分 = 0** → 🟡 **中性**

---

#### 例 4：混合但偏中性
**标题：** "Stock **Rises** Despite **Concerns** About Market"

**计算：**
- `rises` (一般看涨) → **+1**
- `concerns` (一般看跌) → **-1**
- **总分 = 0** → 🟡 **中性**

---

## 🤔 为什么用评分系统？

### ❌ 旧方法的问题（简单匹配）
```typescript
if (title.includes('rise')) → bullish
```

**缺陷：**
- "Stock Rises Despite Huge **Crash** in Market" → 误判为看涨
- 无法区分 "rise" 和 "surge" 的强度差异
- 忽略多个关键词的累积效应

### ✅ 新方法的优势（加权评分）
```typescript
score += 2 for 'surge'
score -= 2 for 'crash'
→ Total = 0 (neutral)
```

**优点：**
1. **考虑多个关键词**：累计所有信号
2. **区分强度**：surge (+2) 比 rise (+1) 更强
3. **平衡判断**：正负关键词可以相互抵消
4. **更准确**：阈值 ±2 避免误判

---

## 📈 实际效果

### TSLA (特斯拉) 测试结果
```
总计 10 条新闻：
🔴 看涨: 2 条  (20%)
🟡 中性: 8 条  (80%)
🟢 看跌: 0 条  (0%)

例子：
🔴 "Cantor Fitzgerald Boosts Tesla Price Target"
   → boost (+1) + target rise implied (+1) = +2 → 看涨
   
🟡 "Elon Musk Calls Jeff Bezos 'Copycat'"
   → 无明确财务关键词 = 0 → 中性
```

---

## 🛠️ 如何调整？

如果你觉得判断不够准确，可以调整：

### 1. 修改阈值
```typescript
// 当前阈值
if (score >= 2) → bullish
if (score <= -2) → bearish

// 更严格（需要更多证据）
if (score >= 3) → bullish
if (score <= -3) → bearish

// 更宽松（更敏感）
if (score >= 1) → bullish
if (score <= -1) → bearish
```

### 2. 添加新关键词
在 `app/api/fomo-meter/route.ts` 中修改：
```typescript
const strongBullish = [
  '你的新关键词',
  ...
];
```

### 3. 调整权重
```typescript
strongBullish.forEach(word => {
  if (title.includes(word)) score += 3; // 改为 +3
});
```

---

## 🎓 为什么不用 AI 判断每条新闻？

### 原因

1. **成本高**：每次分析 10 条新闻，需要调用 OpenAI 10 次
   - 每次 $0.02 → 总计 $0.20 per request
   - 现在只调用 1 次（整体情绪）→ $0.02 per request

2. **速度慢**：10 次 API 调用 → 延迟 10-20 秒
   - 现在关键词匹配 → 瞬时完成

3. **准确度够**：关键词评分系统对于标题判断已经很准确
   - 标题通常包含明确的关键词（升/跌）
   - AI 优势在于理解复杂语境，但标题通常很直接

### 当前方案

- **整体情绪**：用 GPT-4o 分析（深度理解）
- **单条新闻**：用关键词评分（快速标记）

---

## 📊 技术细节

### 代码位置
`app/api/fomo-meter/route.ts` (第 308-348 行)

### 实现伪代码
```typescript
function classifyHeadline(title: string): Sentiment {
  let score = 0;
  
  // 加分
  for (word in strongBullishWords) {
    if (title.includes(word)) score += 2;
  }
  for (word in bullishWords) {
    if (title.includes(word)) score += 1;
  }
  
  // 减分
  for (word in strongBearishWords) {
    if (title.includes(word)) score -= 2;
  }
  for (word in bearishWords) {
    if (title.includes(word)) score -= 1;
  }
  
  // 判断
  if (score >= 2) return 'bullish';
  if (score <= -2) return 'bearish';
  return 'neutral';
}
```

---

## 🎯 总结

| 方面 | 说明 |
|------|------|
| **原理** | 加权评分系统 |
| **阈值** | ±2 分 |
| **准确度** | ~80-90% (基于标题) |
| **速度** | 瞬时（无 API 调用） |
| **成本** | $0（无额外费用） |
| **可调** | 支持自定义关键词和阈值 |

---

**现在你知道每条新闻旁边的 🔴🟡🟢 是怎么来的了！** 🎉

